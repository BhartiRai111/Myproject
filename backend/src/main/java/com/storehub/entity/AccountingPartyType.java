package com.storehub.entity;

/** Optional tag on a JournalDetail line so a control account (e.g. Customer Receivable) can be drilled down by party. */
public enum AccountingPartyType {
    CUSTOMER,
    SUPPLIER
}
